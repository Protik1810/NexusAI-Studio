# Dependencies
node_modules/
.pnp
.pnp.js

# Build & Distribution outputs
dist/
dist-app/
release/
release-pkg/
build/
# Generated by scripts/build-blob-landing.js — the Pages workflow builds and
# deploys this fresh on every push instead of relying on a committed copy.
# Everything in docs/ is generated output (index.html plus the copied
# themes/screenshots/avatars/logo/favicon it references).
docs/
*.exe
*.dll
*.node
*.asar
*.dylib
# macOS engine binaries (llama-server, sd-cli + dylibs) — same as backend/win/,
# these are large local build inputs fetched from upstream releases, not
# committed to git.
backend/mac/

# Generated showcase page (canonical source: docs/index.html, see
# scripts/build-blob-landing.js) — regenerate locally, don't commit a copy.
/product.html

# Local AI Models (Never commit heavy weights to git)
models/**/*.safetensors
models/**/*.gguf
models/**/*.ckpt
models/**/*.bin
models/**/*.pt
llm-models/**/*.gguf
llm-models/**/*.bin
*.safetensors
*.gguf
*.ckpt

# Cache & Local State
.scan_cache.json
models/.scan_cache.json
models/custom_paths.json
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.solframe/

# Outputs
public/outputs/
outputs/
*.png
*.jpg
*.jpeg
*.webp
!public/logo.png
!public/solframe-icon.png
!public/favicon.png
!public/themes/*.jpg
!public/avatars/*.jpg
!public/screenshots/*.webp
!public/themes/hero/*.webp
!public/themes/showcase/*.webp
!public/themes/emblem/*.webp
!electron/icon.png
!electron/icon.ico

# Tauri / Electron cache
src-tauri/target/
.electron/

# OS and Editor files
.DS_Store
Thumbs.db
*.lnk
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Test output images
test_*.png

installer-output/
