## What does this PR do, and why?

<!-- Focus on the "why" — the diff already shows the "what". -->

## Testing

- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] If this touches `electron/engine/*.cjs`: added/updated a test in `src/tests/`
- [ ] If this touches image generation or LLM chat: verified against real engine binaries, not just that it type-checks (see [CONTRIBUTING.md](../CONTRIBUTING.md#a-note-on-the-inference-engines))

## Screenshots (if UI-facing)
